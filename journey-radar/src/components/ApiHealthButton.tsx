// Re-export shim to maintain backward compatibility if any imports still reference this path.
export { default } from '../../components/api-health-button';
export { ApiHealthButton } from '../../components/api-health-button';

